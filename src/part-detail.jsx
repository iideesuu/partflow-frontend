import React,{useEffect,useState} from 'react';
import {apiRequest} from './api.js';
import {Badge,ErrorBox,PageHead,uploadAttachment,list} from './ui.jsx';
const dt=x=>x?new Date(x).toLocaleString('zh-CN'):'—'; const states={draft:'草稿',pending_review:'待审核',rejected:'已驳回',approved:'已批准',release_pending:'待发布',release_failed:'发布失败',released:'已发布',obsolete:'已废止'};
export function PartDetailPage({partId,navigate}){const [part,setPart]=useState(null),[events,setEvents]=useState([]),[tab,setTab]=useState('overview'),[error,setError]=useState(null),[busy,setBusy]=useState(false),[attachment,setAttachment]=useState(null),[progress,setProgress]=useState(0),[uploading,setUploading]=useState(false);const load=async()=>{setError(null);try{const [p,t]=await Promise.all([apiRequest(`/parts/${partId}`),apiRequest(`/parts/${partId}/timeline`)]);setPart(p);setEvents(list(t))}catch(e){setError(e)}};useEffect(()=>{void load()},[partId]);const revs=part?.revisions||[],latest=[...revs].sort((a,b)=>(b.revision_seq||0)-(a.revision_seq||0))[0];const transition=async action=>{if(!latest)return;let reason='';if(action==='reject'){reason=window.prompt('请输入驳回原因')||'';if(!reason)return}setBusy(true);try{await apiRequest(`/parts/${partId}/revisions/${latest.id}/actions`,{method:'POST',body:{action,reason},rowVersion:latest.row_version,idempotencyKey:crypto.randomUUID()});await load()}catch(e){setError(e)}finally{setBusy(false)}};const attach=async()=>{if(!attachment||!latest)return;setUploading(true);try{const s=await uploadAttachment(attachment,setProgress);await apiRequest(`/parts/${partId}/attachments`,{method:'POST',body:{revision:latest.id,upload_session:s.id},idempotencyKey:crypto.randomUUID()});setAttachment(null);await load()}catch(e){setError(e)}finally{setUploading(false)}};if(!part&&!error)return <div className="page"><div className="empty-panel card">加载中…</div></div>;return <div className="page"><PageHead eyebrow="物料主数据 · 详情" title={part?.part_code||'物料详情'} desc={latest?.name||'—'}/><div className="toolbar"><button className="btn secondary" onClick={()=>navigate('parts')}>返回列表</button>{latest?.revision_state==='draft'&&<button className="btn primary" disabled={busy} onClick={()=>transition('submit')}>提交审核</button>}{latest?.revision_state==='pending_review'&&<><button className="btn primary" disabled={busy} onClick={()=>transition('approve')}>批准</button><button className="btn secondary" disabled={busy} onClick={()=>transition('reject')}>驳回</button></>}{latest?.revision_state==='rejected'&&<button className="btn primary" disabled={busy} onClick={()=>transition('revise')}>重新编辑</button>}{latest?.revision_state==='approved'&&<button className="btn primary" disabled={busy} onClick={()=>transition('publish')}>申请发布</button>}{latest?.revision_state==='release_pending'&&<button className="btn primary" disabled={busy} onClick={()=>transition('publish')}>发布</button>}{latest?.revision_state==='release_failed'&&<button className="btn primary" disabled={busy} onClick={()=>transition('retry_publish')}>重试发布</button>}{latest?.revision_state==='released'&&<button className="btn secondary" disabled={busy} onClick={()=>transition('retire')}>废止</button>}</div><ErrorBox error={error}/><div className="tabs"><button className={tab==='overview'?'active':''} onClick={()=>setTab('overview')}>主数据</button><button className={tab==='revisions'?'active':''} onClick={()=>setTab('revisions')}>Revision ({revs.length})</button><button className={tab==='timeline'?'active':''} onClick={()=>setTab('timeline')}>时间线</button><button className={tab==='attachments'?'active':''} onClick={()=>setTab('attachments')}>图纸与附件</button></div>{tab==='overview'&&<section className="card"><div className="detail-grid">{[['状态',part?.status],['Revision',latest?.revision],['类型',latest?.kind],['生命周期',latest?.business_lifecycle],['标准号',latest?.standard_code],['材料',latest?.material],['制造商',latest?.manufacturer],['制造商料号',latest?.manufacturer_part_number],['RoHS',latest?.rohs_standard],['单位',latest?.unit_code]].map(([k,v])=><div key={k}><span className="muted">{k}</span><b>{v||'—'}</b></div>)}</div><p>{latest?.description||'暂无描述'}</p></section>}{tab==='revisions'&&<section className="card"><table><thead><tr><th>Revision</th><th>状态</th><th>生命周期</th><th>附件</th><th>创建时间</th></tr></thead><tbody>{revs.map(r=><tr key={r.id}><td className="mono strong">{r.revision}</td><td><Badge tone={r.revision_state==='released'?'green':r.revision_state==='rejected'?'red':'blue'}>{states[r.revision_state]||r.revision_state}</Badge></td><td>{r.business_lifecycle||'—'}</td><td>{r.attachment_count||0}</td><td>{dt(r.created_at)}</td></tr>)}</tbody></table></section>}{tab==='timeline'&&<section className="card timeline">{events.map((e,i)=><div className="timeline-item" key={e.id||i}><b>{e.action||'操作'}</b><span>{e.actor||'系统'} · {dt(e.created_at)}</span><p>{e.details?JSON.stringify(e.details):''}</p></div>)}{!events.length&&<div className="empty">暂无审计事件</div>}</section>}{tab==='attachments'&&<section className="card empty-panel"><h2>图纸与附件</h2><p>附件通过 MinIO 分片通道上传，单文件上限 5 GiB。</p><div className="form-actions"><input type="file" onChange={e=>setAttachment(e.target.files?.[0]||null)} disabled={uploading||latest?.revision_state!=='draft'}/><button className="btn primary" disabled={!attachment||uploading||latest?.revision_state!=='draft'} onClick={()=>void attach()}>{uploading?`上传 ${progress}%`:'上传并绑定'}</button></div><button className="btn secondary" onClick={()=>navigate('jobs')}>前往文件中心</button></section>}</div>}

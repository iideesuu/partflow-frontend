const base=(import.meta.env.VITE_API_BASE_URL||'/api/v1').replace(/\/$/,'');
export async function apiRequest(path,{method='GET',body,rowVersion,idempotencyKey,...options}={}){const headers={Accept:'application/json',...(body?{'Content-Type':'application/json'}:{}),...(options.headers||{})};const csrf=document.cookie.match(/(?:^|; )csrftoken=([^;]+)/)?.[1];if(csrf&&!['GET','HEAD','OPTIONS'].includes(method))headers['X-CSRFToken']=decodeURIComponent(csrf);if(rowVersion)headers['If-Match']=rowVersion;if(idempotencyKey)headers['Idempotency-Key']=idempotencyKey;const raw=path.startsWith('/')?path:`/${path}`;const [p,q]=raw.split('?');const url=`${base}${p.endsWith('/')?p:`${p}/`}${q?`?${q}`:''}`;const response=await fetch(url,{...options,method,headers,credentials:'include',body:body?JSON.stringify(body):undefined});if(!response.ok){const e=await response.json().catch(()=>({}));throw Object.assign(new Error(e.message||e.detail||`API request failed (${response.status})`),{status:response.status,details:e})}return response.status===204?null:response.json()}
export const endpoints={categories:'/categories',units:'/units',parts:'/parts',boms:'/boms',uploadSessions:'/attachments/upload-sessions',imports:'/imports',exports:'/exports',authCsrf:'/auth/csrf',authLogin:'/auth/login',authMe:'/auth/me',authLogout:'/auth/logout',authUsers:'/auth/users'};
